PREFIX => ctrl + b

start tmux session  ~$ tmux 
start new session with name ~$ tmux new -s <session_name>
attach to session ~$ tmux a 
attach to named sesssion ~$ tmux a -t <session_name>
list sessions ~$ tmux ls 
kill session ~$ tmux kill-session -t <session_name>

k   kill session

WINDOWS 

c  create window 
w  list windows
n  next window
p  previous window
f  find window 
,  name window
&  kill window

PANES 

%  vertical split
"  horizontal split
o  swap panes
q  show pane numbers
x  kill pane
s  show sessions
{  Move the current pane left
}  Move the current pane right
z  toggle pane zoom
[  enable scroll
c  synchronize panes (add this 'bind s setw synchronize-panes' to the .tmux.conf file and then do ctrl+b :source-file ~/.tmux.conf) 

ALT + arrow_key (Resizes the current pane in arrow_key direction)
prefix + arrow_key      move to the pane in the direction

MISC 

d  detach
t  big clock
?  list shortcuts
:  prompt
