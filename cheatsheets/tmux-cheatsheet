PREFIX => ctrl + b

start tmux session  ~$ tmux 
start new session with name ~$ tmux new -s <session_name>
attach to session ~$ tmux a 
attach to named sesssion ~$ tmux a -t <session_name>
list sessions ~$ tmux ls 
kill session ~$ tmux kill-session -t <session_name>

WINDOWS 

c  create window 
w  list windows
n  next window
p  previous window
f  find window 
,  name window
&  kill window

PANES 

%  vertical split
"  horizontal split
o  swap panes
q  show pane numbers
x  kill pane
{  Move the current pane left
}  Move the current pane right
z  toggle pane zoom
[  enable scroll
s  synchronize panes (add this 'bind s setw synchronize-panes' to the .tmux.conf file and then do ctrl+b :source-file ~/.tmux.conf) 

ALT + arrow_key (Resizes the current pane in arrow_key direction)

MISC 

d  detach
t  big clock
?  list shortcuts
:  prompt
